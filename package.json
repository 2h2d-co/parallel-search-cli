{
  "name": "parallel-search-cli",
  "version": "0.1.1-alpha.1",
  "description": "An unofficial zero-runtime-dependency CLI for the Parallel Search and Extract APIs",
  "homepage": "https://github.com/2h2d-co/parallel-search-cli#readme",
  "bugs": {
    "url": "https://github.com/2h2d-co/parallel-search-cli/issues"
  },
  "license": "MIT",
  "author": {
    "name": "Kaan Ozdokmeci",
    "email": "kaan@2h2d.co",
    "url": "https://www.2h2d.co"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/2h2d-co/parallel-search-cli.git"
  },
  "bin": {
    "parallel-search": "bin/parallel-search.js"
  },
  "files": [
    "bin",
    "dist",
    "src",
    "skills",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test --test-concurrency=1 test/*.test.ts",
    "test:integration": "npm test && node --test --test-concurrency=1 test/integration/*.test.ts",
    "release": "node scripts/release.ts",
    "fmt": "oxfmt",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "check": "hk check --all --check",
    "pack:dry": "npm run build && npm pack --dry-run --allow-directory=all"
  },
  "devDependencies": {
    "@2h2d/oxlint-config": "0.1.0-alpha.1",
    "@types/node": "22.20.1",
    "oxfmt": "0.63.0",
    "oxlint": "1.78.0",
    "oxlint-tsgolint": "7.0.2001",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
